import 'package:contractor_search/model/formatted_contact_model.dart';

class FormattedContacts {
  List<String> formattedPhoneNumbers;
  List<FormattedContactModel> formattedContactModelList;

  FormattedContacts(this.formattedPhoneNumbers, this.formattedContactModelList);
}
